{
  "name": "kata-meal-planner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docs:aggregate": "node scripts/aggregate-readmes.cjs",
    "docs:serve": "node scripts/aggregate-readmes.cjs && python -m mkdocs serve",
    "docs:build": "node scripts/aggregate-readmes.cjs && python -m mkdocs build"
  }
}
