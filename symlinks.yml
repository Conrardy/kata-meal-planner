# Symlinks Configuration
# This file documents all symbolic links in the repository (excluding node_modules)
# Generated from repository analysis
# NOTE: All target paths are RELATIVE to the source location

symlinks:
  # Root level symlinks
  - source: CLAUDE.md
    target: AGENTS.md
    description: Claude configuration file pointing to AGENTS.md

  # .claude directory symlinks
  - source: .claude/agents
    target: docs/agents
    description: Claude agents directory pointing to docs/agents

  - source: .claude/commands/custom
    target: docs/prompts
    description: Claude custom commands pointing to docs/prompts

  - source: .claude/commands/flows
    target: docs/flows
    description: Claude flows commands pointing to docs/flows

  - source: .claude/commands/ide
    target: aidd/prompts/ide
    description: Claude IDE commands pointing to aidd/prompts/ide

  # .cursor directory symlinks
  - source: .cursor/commands/ide
    target: aidd/prompts/ide
    description: Cursor IDE commands pointing to aidd/prompts/ide
