Coding assertions (common)

- Keep changes minimal and explicit in prompts and templates.
- Use relative paths with @ when referencing config or code files (e.g., @aidd/prompts/).
- No hard-coded secrets in the repo.
- Prefer concise commit messages and update memory bank only when real code/structure changes.
