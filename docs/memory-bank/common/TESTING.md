Testing (common)

- Where tests live: not detected; search for @package.json scripts to confirm.
- Minimal tests expected for key flows; add tests under a dedicated `tests/` folder.
- Run tests: use `npm test` if present in @package.json.
- Edge cases: empty inputs, invalid prompt formats, missing files.
